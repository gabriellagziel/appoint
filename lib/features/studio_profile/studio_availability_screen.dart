import 'package:flutter/material.dart';
import '../studio_business/screens/business_availability_screen.dart';

class StudioAvailabilityScreen extends StatelessWidget {
  const StudioAvailabilityScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const BusinessAvailabilityScreen();
  }
}
