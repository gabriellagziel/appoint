{
  "name": "business-app",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/ && cp *.html dist/ && echo 'Business app built successfully'",
    "export": "mkdir -p out && cp public/index.html out/index.html",
    "start": "node server.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "http-server": "^14.1.1",
    "jsonwebtoken": "^9.0.2"
  }
}
