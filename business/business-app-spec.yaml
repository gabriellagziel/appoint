name: app-oint-business
region: nyc1

services:
  - name: business-web
    environment_slug: static-site
    github:
      repo: "gabriellagziel/appoint"
      branch: main
    source_dir: "business/public"
    build_command: echo "Static site - no build needed"
    run_command: echo "Static site served by DigitalOcean"
    http_port: 8080
    instance_count: 1
    instance_size_slug: basic-xxs

routes:
  - path: /
    preserve_path_prefix: false
