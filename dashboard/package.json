{
  "name": "app-oint-api",
  "version": "1.0.0",
  "description": "App-oint API Backend",
  "main": "api/index.js",
  "scripts": {
    "start": "node api/index.js",
    "dev": "nodemon api/index.js",
    "build": "echo 'Dashboard API built successfully' && mkdir -p dist && cp -r api dist/",
    "test": "echo \"No tests specified\""
  },
  "dependencies": {
    "@app-oint/design-system": "file:../packages/design-system",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}