import { QuickDashboard } from "@/components/QuickDashboard"

export default function QuickPage() {
  return <QuickDashboard />
}

export const metadata = {
  title: "Quick Dashboard - App-Oint Admin",
  description: "Mobile-optimized admin dashboard for quick access to key metrics"
}