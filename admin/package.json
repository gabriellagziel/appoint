{
  "name": "admin-app",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p out && echo 'Building admin app...'",
    "export": "mkdir -p out && cp public/index.html out/index.html",
    "serve": "http-server out -p ${PORT:-8082}",
    "start": "npm run serve"
  },
  "dependencies": {
    "@app-oint/design-system": "file:../packages/design-system",
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.11"
  }
}
