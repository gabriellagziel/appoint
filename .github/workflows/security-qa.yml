name: Security QA

push:
  branches: [ main ]

on:
  pull_request:
  schedule:
    - cron: '0 0 * * *'

jobs:
  security-qa:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Placeholder Security QA
        run: echo "Security QA stub"