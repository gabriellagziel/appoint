name: Security QA

push:
  branches: [ main ]

on: workflow_dispatch
jobs:
  security-qa:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Placeholder Security QA
        run: echo "Security QA stub"
