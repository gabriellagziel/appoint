name: Release

push:
  branches: [ main ]

on: workflow_dispatch
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Placeholder Release
        run: echo "Release stub"
