name: Android Build

push:
  branches: [ main ]

on:
  pull_request:

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Placeholder Android Build
        run: echo "Android build stub"