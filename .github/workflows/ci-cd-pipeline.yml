name: CI/CD Pipeline

push:
  branches: [ main ]

on: workflow_dispatch
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder step
        run: echo "CI/CD pipeline stub"
